# Cost ledger prices (USD per 1M tokens). Update from provider pricing pages.
# FR-8.x cost controls; ledger reads this file, never hardcoded prices.
models:
  gpt-4o-mini:
    input_per_1m: 0.15
    output_per_1m: 0.60
  gpt-4o:
    input_per_1m: 2.50
    output_per_1m: 10.00
  text-embedding-3-small:
    input_per_1m: 0.02
    output_per_1m: 0.0
